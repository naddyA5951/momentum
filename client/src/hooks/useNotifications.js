import {useCallback, useState} from 'react';
export function useNotifications(){const [enabled,setEnabled]=useState(()=>window.Notification?.permission==='granted');const ping=useCallback(()=>{try{const c=new AudioContext();const o=c.createOscillator(),g=c.createGain();o.frequency.value=660;g.gain.setValueAtTime(.06,c.currentTime);g.gain.exponentialRampToValueAtTime(.001,c.currentTime+.25);o.connect(g).connect(c.destination);o.start();o.stop(c.currentTime+.25)}catch{}} ,[]);const request=async()=>{if(!window.Notification)return;setEnabled((await Notification.requestPermission())==='granted')};const notify=useCallback((title,body)=>{ping();if(Notification.permission==='granted')new Notification(title,{body,icon:'/vite.svg'})},[ping]);return {enabled,request,notify}}
